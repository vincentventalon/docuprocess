{
    "url": "/v1/resources",
    "method": "GET",
    "response": {
        "iterate": "{{body.resources}}",
        "output": {
            "label": "{{item.name}} | {{item.id}}",
            "value": "{{item.id}}"
        }
    }
}
